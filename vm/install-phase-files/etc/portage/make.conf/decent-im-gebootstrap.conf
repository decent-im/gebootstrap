# MAKEOPTS='-j1'
# -user* is a workaround for merging prosody-modules, it failed to lock hg repo directory
FEATURES="${FEATURES} -userfetch -userpriv -usersandbox -usersync"
EMERGE_DEFAULT_OPTS="${EMERGE_DEFAULT_OPTS} --quiet-build --keep-going --getbinpkg --usepkg --buildpkg "
EMERGE_DEFAULT_OPTS="${EMERGE_DEFAULT_OPTS} --tree --unordered-display --nospinner "
